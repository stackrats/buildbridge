// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ProjectLayout } from "./ProjectLayout";
import type { UnsignedBuildTarget } from "./UnsignedBuildTarget";
import type { WorkspaceSource } from "./WorkspaceSource";

export type StoredAppleWorkspace = { localPath: string, name: string, 
/**
 * What the folder holds, as detected when it was approved: the framework in front, the
 * workspace or project Xcode opens, the schemes offered. Records from before detection
 * existed were all Capacitor projects in the one layout that was assumed.
 */
layout: ProjectLayout, 
/**
 * The scheme the builds use, chosen from the layout's or generated for its app target.
 */
scheme: string, developmentTeam: string | null, bundleIdentifier: string | null, lastSnapshotSha256: string | null, lastSyncFileCount: number | null, lastSyncBytes: number | null, 
/**
 * When the snapshot was copied, so a saved snapshot can be told from the folder as it is
 * now; `None` on records from before it was kept.
 */
lastSyncedAtEpochSeconds: number | null, lastBuildSucceeded: boolean, lastXcodeVersion: string | null, lastNativeLockUpdated: boolean, 
/**
 * Which SDK the last unsigned build compiled against; None on records from before the choice.
 */
lastBuildTarget: UnsignedBuildTarget | null, 
/**
 * The identifier resolved from the App target's Debug build settings, even when it is
 * the same as the release identifier. None until device signing or a device run resolves it.
 */
debugBundleIdentifier: string | null, 
/**
 * What the last snapshot was taken from: the approved folder as it was, or a checked-out
 * revision of it requested by a remote build.
 */
lastSource: WorkspaceSource | null, };
